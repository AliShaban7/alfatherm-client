# Copy to .env for local/prod overrides.
# Backend API base URL. Leave empty to use same-origin '/api' (dev proxy).
# When the backend is hosted separately, set the full URL:
# VITE_API_URL=https://your-backend-host.com/api
VITE_API_URL=
